version: "3.9"

services:
  scheduling-env:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: calendar-scheduling-env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./scheduling_env:/app/scheduling_env
    command: python -m scheduling_env.baseline
